# Rust Go .NET Microservice Benchmark 2026 (v1)

The `v1` branch preserves the original benchmark baseline and reference artifacts for the first release cut.

## Included
- `implementations/` initial service implementations
- `artifacts/` baseline benchmark outputs
- initial benchmark spec (`spec.yaml`)

## Channel
- YouTube: https://www.youtube.com/@prod.garbagedestroyer
